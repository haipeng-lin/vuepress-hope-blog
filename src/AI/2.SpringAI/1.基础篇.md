---
title: 基础篇
date: 2026/05/08
---

## 一、前言

​ Spring AI 是 Spring 生态中专为 AI 工程设计的应用框架，简化了与大型语言模型 （LLM）、嵌入模型和向量数据库的集成，让企业级 Java 应用轻松接入 AI 能力，如聊天机器人、RAG（Retrieval Augmented Generation）和智能代理。

优点：

- Java 原生：无需切换到 Python，保持端到端 Java 开发
- Spring 集成：无缝嵌入 Spring Boot，支持自动装配和 Starter
- 多提供商支持：统一 API 接入 OpenAI、Anthropic、Ollama 等，避免供应商锁
- 企业级特性：工具调用、RAG、评估框架，解决幻觉（hallucination）和安全性问题。

相关链接：

- [Spring AI 官网](https://springdoc.cn/spring-ai/)
- [Spring AI 与 SpringBoot 版本关系](https://java2ai.com/docs/versions)

## 二、Hello SpringAI

### 添加依赖

```xml
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-bom</artifactId>
            <version>1.0.0</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>

<dependencies>
    <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-starter-model-openai</artifactId>
    </dependency>
</dependencies>
```

### 配置模型

```yml
spring:
  ai:
    openai:
      api-key: 密钥
      base-url: 模型地址
      chat:
        options:
          model: 模型名称
          temperature: 0.7
          max-tokens: 500
```

### 测试

```java
@RestController
@RequestMapping("/ai")
public class AIController {

    @Autowired
    private OpenAiChatModel openAiChatModel;

    @GetMapping("/chat")
    public String chat(@RequestParam("msg") String msg) {
        return openAiChatModel.call(msg);
    }
}
```

## 三、流式输出

### 配置全局字符集

```yml
server:
  servlet:
    encoding:
      force: true
      enabled: true
      charset: UTF-8
```

### 使用 Flux 响应

```java
@RestController
@RequestMapping("/ai")
public class AIController {

    @Autowired
    private OpenAiChatModel openAiChatModel;

    @GetMapping(path = "/chatStream", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
    public Flux<String> chatStream(@RequestParam("prompt") String prompt) {
        return openAiChatModel.stream(prompt);
    }
}
```

## 四、提示词

### 概念

#### 提示词

SpringAI 中，我们使用 `Prompt` 类来表示一个提示词，`Prompt` 类中包含一个 `List<Message>` 属性，**用于表示提示词中的消息列表**。

```java
public class Prompt implements ModelRequest<List<Message>> {
    // 消息列表
    private final List<Message> messages;

    private ChatOptions chatOptions;
}
```

#### 消息

用户与大模型中间的对话，通常是由一个或多个消息组成，其中每个消息都需要与一个角色关联；角色表示消息作者的角色。

SpringAI 中，我们使用 `Message` 类来表示一个消息，`Message` 类中包含一个 `MessageType` 属性，用于表示消息的类型，通常有四种角色：

- system: 系统角色，用于通过分配特定行为给大模型来创建对话的上下文或范围，通常我们预设的提示词会和 `system` 角色关联
- user: 用户角色，用于表示用户输入的文本，通常我们输入的提问也会和 `user` 角色关联
- assistant: 助手角色，用于表示模型生成的文本，通常大模型生成的答案也会和 `assistant` 角色关联
- tool: 工具角色，用于表示模型调用的函数返回的内容，会和 `tool` 角色关联

```java
public interface Content {

    String getContent();

    Map<String, Object> getMetadata();
}

public interface Message extends Content {
    MessageType getMessageType();
}
```

Spring AI 的 message api：

![](https://img.haipeng-lin.cn/1778289448774.webp)

#### 提示词模板

SpringAI 提供了一种模板语法，用于生成提示词，模板语法如下：

```text
You are a helpful AI assistant that helps people find information.
Your name is {name}
You should reply to the user's request with your name and also in the style of a {voice}.
```

模板语法中，`{name}` 和 `{voice}` 是模板变量，模板变量的值会根据调用时传入的参数进行替换

### 实操

#### 提示词

用法

```java
// 直接创建一个用户的消息提示词
Prompt prompt = new Prompt("xxx");

// 若我们需要创建不同角色的消息，则可以使用
Prompt prompt = new Prompt(new SystemMessage("xxx"), new UserMessage("xxx"));

// 若希望指定模型调用的参数，则需要使用 ChatOptions
Prompt prompt = new Prompt(text, ZhiPuAiChatOptions.builder().temperature(0.7d).build());
```

例子：

```java
@RestController
@RequestMapping("/ai")
public class AIController {

    @Autowired
    private OpenAiChatModel openAiChatModel;

    @GetMapping("/chatPrompt")
    public String chatPrompt(@RequestParam("message") String message) {
        Prompt prompt = new Prompt(
                Arrays.asList(new SystemMessage("你现在是一位售后服务的客服"), new UserMessage(message))
        );
        Generation generation = openAiChatModel.call(prompt).getResult();
        return generation.getOutput().getText();
    }
}
```

#### 提示词模板

用法：

```java
PromptTemplate promptTemplate = new PromptTemplate("xxx{value1},xxx{value2}")
Prompt prompt = promptTemplate.create(Map.of("value1", value1, "value2", value2));	// 默认 UserMessage 类型的消息
```

创建 SystemMessage 类型的消息

```java
SystemPromptTemplate promptTemplate = new SystemPromptTemplate("xxx{value1},xxx{value2}");
Message systemMsg = promptTemplate.createMessage(Map.of("value1", value1, "value2", value2));
Prompt prompt = new Prompt(systemMsg, new UserMessage(msg));
```

## 五、结构化输出

### BeanOutputConverter

作用：用于将 LLM（大语言模型）返回的非结构化字符串（String）内容，自动解析并转换为 Java 中的结构化对象（POJO/Bean）

用法：

```java
// 创建转换器
BeanOutputConverter<类> beanOutputConverter = new BeanOutputConverter<>(类.class);

// 获取格式化指令并加入到 prompt
String format = beanOutputConverter.getFormat();
PromptTemplate template = new PromptTemplate("帮我返回五个{actor}"主演的电影名{format});
template.create(Map.of("actor",actor,"format",format))

// 调用模型并转换结果
String response = chatModel.call(prompt).getResult().getOutput().getText();
类 类名 =  beanOutputConverter.convert(response);
```

例子：

```java
@GetMapping("/chatBeanOutput")
public ActorFilm chatBeanOutput(@RequestParam("actor") String actor) {
    BeanOutputConverter<ActorFilm> beanOutputConverter = new BeanOutputConverter<>(ActorFilm.class);
    String format = beanOutputConverter.getFormat();

    // 1. 在 Prompt 中明确要求
    PromptTemplate promptTemplate = new PromptTemplate(
        "帮我找{actor}主演的五部电影。注意：只输出 JSON 内容，严禁包含任何推理过程、解释或 Markdown 标记。{format}"
    );
    Prompt prompt = promptTemplate.create(Map.of("actor", actor, "format", format));

    // 2. 获取原始响应
    String rawText = openAiChatModel.call(prompt).getResult().getOutput().getText();

    // 3. 执行强制清洗逻辑
    String cleanedText = repairJson(rawText);

    if (cleanedText.isEmpty()) {
        throw new RuntimeException("AI 返回内容为空或无法识别 JSON");
    }

    return beanOutputConverter.convert(cleanedText);
}

/**
 * 强力修复 JSON 字符串：只保留第一个 { 和最后一个 } 之间的内容
*/
private String repairJson(String text) {
    if (text == null || text.isBlank()) return "";

    // 尝试寻找第一个 {
    int start = text.indexOf("{");
    // 尝试寻找最后一个 }
    int end = text.lastIndexOf("}");

    if (start != -1 && end != -1 && start < end) {
        return text.substring(start, end + 1);
    }

    return ""; // 如果连括号都找不着，说明数据彻底坏了
}
```

### MapOutputConverter

作用：将大模型返回的字符串内容自动解析并转换为 Java 中的 Map 键值对

用法：同 BeanOuputConverter

## 六、对话上下文（未）

## 七、工具调用（FunctionCalling）

### 调用流程

1. 在向大模型发起请求时，将工具信息（名称、描述、输入参数）传递给大模型
2. 大模型根据工具信息，会发送包含工具名称、亲求参数的请求给工具服务
3. 应用程序根据工具名称，识别到对应的工具，调用工具方法
4. 工具执行结果返回给应用程序进行处理（可能是直接返回给用户，也可能是返回给大模型）
5. 应用程序将工具执行结果返回给大模型
6. 大模型利用工具返回的结果，构建返回结果给用户

### Spring AI 实现

两种实现方式：

- 声明式：
  - 实现方法：注解 `@Tool`
  - 适用场景：工具实现完全由开发者掌控
- 编程式：
  - 实现方法：底层的 `MethodToolCallback` `FunctionToolCallback`
  - 适用场景：非控制的工具实现（如三方 sdk），或者需要动态获取工具参数

#### 声明式

```java
public class DateTimeTools {

    /**
     * 获取当前时间
     *
     * @return 结果
     */
    @Tool(description = "获取用户的当前日期和时间")
    String getCurrentDateTime() {
        String ans = LocalDateTime.now().atZone(LocaleContextHolder.getTimeZone().toZoneId()).toString();
        System.out.println("进入获取当前时间了：" + ans);
        return ans;
    }

}
```

带参数的工具方法：

```java
@Tool(description = "传入时区，返回对应时区的当前时间给用户")
String getTimeByZoneId(@ToolParam(description = "需要查询时间的时区") ZoneId area) {
    // 根据时区，查询对应的时间
    ZonedDateTime time = LocalDateTime.now().atZone(area);

    // 转换为 2025-07-26 20:00:00 格式的字符串
    // 将输入时区的时间转换为本地时区
    ZonedDateTime localTime = time.withZoneSameInstant(ZoneId.systemDefault());
    DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");
    String ans = localTime.format(formatter);
    System.out.println("传入的时区是：" + area + "-" + ans);
    return ans;
}
```

测试：

```java
@GetMapping("/time")
public String getTime(String msg) {
    return chatClient.prompt(msg).tools(new DateTimeTools()).call().content();
}

@GetMapping("/timeNoTools")
public String getTimeNoTools(String msg) {
    return chatClient.prompt(msg).call().content();
}
```

#### 编程式（未）

## 八、ChatClient

### 基本使用

#### 创建 ChatClient

- SpringBoot 自动装配的`ChatClient.Builder`（项目只有一个大模型）

  ```java
  @RestController
  public class ChatController {
      private final ChatClient chatClient;

      public ChatController(ChatClient.Builder builder) {
          chatClient = builder.build();
      }
  }
  ```

- ChatModel

  ```java
  @RestController
  public class ChatController {

      private final ChatClient chatClient;

      public AIController(OpenAiChatModel chatModel) {
          this.chatClient = ChatClient.builder(chatModel)
                  .build();
      }
  }
  ```

#### 传入提示词

使用 chatClient.prompt() 方法有以下三种方式传入提示词

- **接收 String**

  ```java
  chatClient.prompt("给我讲个笑话").call().content()
  ```

- **接收 Prompt 对象**

  ```java
  chatClient.prompt(new Prompt(new UserMessage("给我讲个笑话"))).call().content();
  ```

- **Fluent 式**

  ```java
  chatClient.prompt()
      .system("你是一名段子手")
      .user("给我讲个笑话")
      .call().content();
  ```

#### 响应

AI 模型返回的 `ChatResponse` 对象，封装了模型返回的 `Generation` 对象，以及一些元数据、token 统计

```java
ChatResponse response = chatClient.prompt()
    .system("你是一名段子手")
    .user("给我讲个笑话")
    .call().chatResponse();

// token 统计
Usage usage = response.getMetadata().getUsage();
// 返回的消息
Generation generation = response.getResult();
// 结构化输出：entity()
Joke joke = chatClient.prompt()
    .system("你是一名段子手")
    .user("给我讲个笑话")
    .call().entity(Joke.class);
```

#### 流式输出

用法：使用 stream() 替代 call()

```java
@GetMapping(path = "/ai/fluxGen", produces = "text/event-stream")
public Flux<String> fluxGen(@RequestParam(value = "msg", defaultValue = "你好") String msg) {
    return chatClient.prompt()
            .system("你是一名段子手")
            .user(msg).stream().content();
}
```

### 进阶使用

#### 提示词模板

`Fluent` 式 API 支持提供含变量的用户/系统消息模板

用法：text("提示词模板").param("参数名",参数值);

```java
@GetMapping("/ai/template")
public String template(@RequestParam(value = "role", defaultValue = "李白") String role,
                       @RequestParam(value = "msg", defaultValue = "你好") String msg) {
    return chatClient.prompt()
            .system(u -> u.text("你现在扮演盛唐著名的诗人{role}，接下来我们进行对话")
                    .param("role", role))
            .user(u -> u.text("我是一个现代诗歌爱好者，我的提问是：{msg}").params(Map.of("msg", msg)))
            .call().content();
}
```

#### 流式结构化返回

用法：BeanOutputConverter

```java
public class ChatController {

    @GetMapping(path = "/ai/fluxGenV2")
    public List<Poem> fluxGenV2(@RequestParam(value = "msg", defaultValue = "你好") String msg) {
        var converter = new BeanOutputConverter<>(new ParameterizedTypeReference<List<Poem>>() {
        });

        Flux<String> flux = chatClient.prompt()
                .system("你现在扮演盛唐著名的诗人李白，接下来我们进行对话")
                .user(u -> u.text("{msg}.\n{format}").param("msg", msg).param("format", converter.getFormat()))
                .stream().content();
        String content = flux.collectList().block().stream().collect(Collectors.joining());
        return converter.convert(content);
    }
}
```

## 九、语音识别

使用的模型

- 硅基的免费语音识别模型
- MiniMax 的 Mini-M2.7 文本模型

### 引入依赖

```xml
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-model-openai</artifactId>
</dependency>

```

### 配置模型

```yml
spring:
  ai:
    openai:
      # 全局 api-key 兜底
      api-key: sk-cp-3c5Z4KFlfbZ6p8N

      # 1. 语音识别专用配置 (SiliconFlow)
      audio:
        transcription:
          base-url: https://api.siliconflow.cn
          api-key:
          options:
            model: FunAudioLLM/SenseVoiceSmall
            response-format: text

      # 2. 聊天对话专用配置 (Minimax)
      chat:
        base-url: https://api.minimaxi.com
        api-key: sk-cp-3c5Z4KFlfbZ6p8N
        options:
          model: MiniMax-M2.7
          temperature: 0.7
          max-tokens: 500
```

### 测试

```java
@RestController
public class AudioTransactionController {
    private Logger log = org.slf4j.LoggerFactory.getLogger(AudioTransactionController.class);

    /**
     * 直接使用默认注册的模型对象来进行交互
     */
    @Autowired
    private TranscriptionModel initTranscriptionModel;


    @Value("classpath:/test.mp3")
    private Resource resource;

    /**
     * 使用OpenAI风格的语言识别
     *
     * @return
     * @throws IOException
     */
    @GetMapping(path = "translateAudio")
    @ResponseBody
    public Object translateAudio() throws IOException {
        AudioTranscriptionPrompt prompt = new AudioTranscriptionPrompt(resource);
        AudioTranscriptionResponse response = initTranscriptionModel.call(prompt);
        log.info("response -> {}", response);
        return response.getResult().getOutput();
    }

}
```
